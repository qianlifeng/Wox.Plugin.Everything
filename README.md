# Everything

Windows x64 Wox plugin that searches files through Everything DLL APIs.

Supported runtime behavior:

- Prefer `Everything 1.5` via `Everything3_x64.dll`
- Fallback to `Everything 1.4` via `Everything64.dll`
- Minimal action set: search and open

# Install

```
wpm install Everything
```
